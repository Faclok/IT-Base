meta:
  environment: python
  toolchain:
    name: pip
    version: "3.11"

run:
  script: cd backend && pip install -r requirements.txt && uvicorn app:app --host 0.0.0.0 --port ${PORT}
  persistenceMount: /data