meta:
  environment: python
  toolchain:
    name: pip
    version: "3.11"

build:
  commands:
    - pip install -r backend/requirements.txt

run:
  command: cd backend && uvicorn app:app --host 0.0.0.0 --port ${PORT}