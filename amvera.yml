meta:
  environment: python
  toolchain:
    name: pip
    version: "3.11"

run:
  build:
    - pip install -r backend/requirements.txt
  script: cd backend && uvicorn app:app --host 0.0.0.0 --port $PORT
  persistenceMount: /data